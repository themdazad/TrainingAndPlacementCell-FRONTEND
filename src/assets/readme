
The 'assets' folder is used to store static resources such as images, icons, fonts, and static data files (e.g., JSON, markdown, or text files) that are required by the application.

These files are not processed by the build tools and are served as-is to the client.